# Runtime Check — Required

Live URL:

https://stackblitz.com/github/maloltzahi-byte/kaddishim-shell

## Required checks

1. Open the StackBlitz URL.
2. Wait until dependencies finish installing.
3. Confirm the Vite dev server starts.
4. Confirm the dashboard renders from React.
5. Open the browser console and confirm there are no runtime errors.
6. Capture a screenshot at 1440px width and save it as `qa/dashboard-v3-real.png` in the next commit.
7. Run visual overlay against the source dashboard crop.

## Current status

The repository is populated with a React/Vite implementation. The runtime screenshot is not yet committed because the current execution environment cannot open StackBlitz and produce a browser screenshot.
